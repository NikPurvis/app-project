{% layout 'layout.liquid' %}
{% block content %}

<h1>Add song to setlist</h1>

<div class="form-container">
    <form action="/setlist/create" method="POST">

        <input type ="hidden" id="request" name="request" value="{{ song.id }}">

        <p><label>Song:
            <input
                type="text"
                name="title"
                placeholder="{{ song.title }}"
                readonly
            >
        </label></p>

        <p><label>Artist:
            <input
                type="text"
                name="artist"
                placeholder="{{ song.artist }}"
                readonly
            >
        </label></p>

        <p><label for="position"><span>Position</span></label>
        <select class="" name="position" id="position">
            {% if song.position contains "Vocals" %}
                <option value="Vocals">Vocals</option>
            {% endif %}
            {% if song.position contains "Guitar" %}
                <option value="Guitar">Guitar</option>
            {% endif %}
            {% if song.position contains "Bass" %}
                <option value="Bass">Bass</option>
            {% endif %}
            {% if song.position contains "Drums" %}
                <option value="Drums">Drums</option>
            {% endif %}
            {% if song.position contains "Keys" %}
                <option value="Keys">Keys</option>
            {% endif %}
        </select></p>
        <p>
            <label for="comment">Hello, Cleveland!</label><br />
            <textarea id="comment" name="comment" rows="4" cols="50" placeholder="FREEBIRD"></textarea>
        </p>
        <input type="submit" value="Add song to setlist">
    </form>
</div>


{% endblock %}